pydantic>=2.0.0
opencv-python
pillow
numpy
torch
torchvision
huggingface_hub
datasets
scikit-learn
tqdm
ipykernel
ipywidgets
transformers
timm
safetensors
pyarrow
accelerate>=1.1.0
faiss-cpu
matplotlib
pandas
seaborn
pytest
# Official C2PA Python bindings (Content Authenticity Initiative / Adobe).
# Used by src/genai_detection/metadata_module/provenance_validation.py
# to cryptographically validate C2PA manifests. Missing metadata or a
# missing validator both surface as ProvenanceStatus values — the app
# runs without it (validator_unavailable), it is only required for
# actual provenance validation.
# Tested locally against c2pa-python 0.37.7 (see constraints.txt).
c2pa-python>=0.37,<0.38
# Adobe TrustMark — scheme-specific watermark detector wrapped by
# src/genai_detection/watermark_module/. Import and model download are
# lazy (first analysis call), so a missing package or unreachable
# model host cannot block app startup — the watermark card just
# reports status=detector_unavailable.
# Tested locally against trustmark 0.9.1 (see constraints.txt).
trustmark>=0.9,<1.0

